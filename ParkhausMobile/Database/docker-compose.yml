services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: parkhaus-sql
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Igid1KRD*"
    ports:
      - "1433:1433"
    volumes:
      - parkhaus_sql_data:/var/opt/mssql

volumes:
  parkhaus_sql_data:
